[build]
  base = "frontend"
  command = "cd frontend && npm install && npm run build && npm run export"
  publish = "frontend/out"

[build.environment]
  NODE_VERSION = "20"
  NEXT_PUBLIC_API_BASE_URL = "https://your-backend-url.com"

[[plugins]]
  package = "@netlify/plugin-nextjs"
